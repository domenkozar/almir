language: python
python:
  - 2.6
  - 2.7
install:
  - sudo apt-get install libevent-dev git gcc python-dev
  - cp buildout.d/templates/buildout.cfg.in buildout.cfg
  - python bootstrap.py
  - bin/buildout
script: nosetests

notifications:
  irc:
    - "irc.freenode.org#almir"
