# you should never edit this file, but the template located at
# buildout.d/templates/bconsole.conf.in

Director {
  Name = ${almir:director_name}
  DIRport = ${almir:director_port}
  address = ${almir:director_address}
  Password = "${almir:director_password}"
}
